#include <iostream>
using namespace std;
int main()
{
    int n;
    cin >> n;

    unsigned int first, second, final, credits;
    unsigned short A = 0, B = 0;
    double GPA = 0;

    while (n--)
    {
        cin >> first >> second >> final >> credits;
        B += credits;
    }

    if (first + second >= 30){

    }
    /* formula of GPA is A / B
    when A is GPA of every discipline 
    when B is count of credits
    */
}